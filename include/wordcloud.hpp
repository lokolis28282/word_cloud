#pragma once

#include <string>
#include <vector>

using namespace std;

struct ww {
    string s;
    int n;
};

string aa(string x);
vector<ww> bb(vector<string> x);
bool dd(vector<string> x, string f);
